1️⃣ Virtual Environment Setup (Windows)
python -m venv venv
Windows: venv\Scripts\activate 
ubantu: source venv/bin/activate

2️⃣ Install Dependencies
pip install -r requirements.txt
pip freeze > requirements.txt

3️⃣ PostgreSQL Setup
Create database in pgAdmin:
Database name: tour

Update alembic.ini:
sqlalchemy.url = postgresql://postgres:<PASSWORD>@localhost:5432/tour

4️⃣ Initialize Alembic (Only Once)
alembic init alembic

5️⃣ Create Migration (After Model Change)
alembic revision --autogenerate -m "migration message"
Example:
alembic revision --autogenerate -m "make booking vehicle and driver table"

6️⃣ Apply Migration to Database
alembic upgrade head

7️⃣ Downgrade Migration (If Needed)
alembic downgrade -1

8️⃣ Run Backend Server
uvicorn app.main:app --reload
Server : http://127.0.0.1:8000
API Docs : http://127.0.0.1:8000/docs

9️⃣ Verify Tables (pgAdmin Query Tool)
SELECT * FROM agents;

10. Create Super Admin User
(venv) C:\xampp\htdocs\tourbot\backend>python -m app.seeds.create_super_admin

11. run ngrok Server
uvicorn app.main:app --reload --port 8000
ngrok http 8000
server : uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

sudo nano /etc/systemd/system/tourbot.service
after take pull
sudo systemctl restart gunicorn
sudo systemctl restart nginx
sudo systemctl restart tourbot

12. Setup WhatsApp Webhook
Go to Meta Developer Dashboard → Your App → WhatsApp → Configure → Webhooks
Copy ngrok URL → Add endpoint: /webhooks/whatsapp
Verify token: my_whatsapp_webhook_token


ssl : sudo nano /etc/nginx/sites-available/tourbot

# Redirect all HTTP requests to HTTPS
server {
    listen 80;
    server_name tourbookingaiagent.coderscotch.com;
    return 301 https://$host$request_uri;
}

# Main HTTPS server
server {
    listen 443 ssl;
    server_name tourbookingaiagent.coderscotch.com;

    # SSL certs from Let's Encrypt
    ssl_certificate /etc/letsencrypt/live/tourbookingaiagent.coderscotch.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/tourbookingaiagent.coderscotch.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Serve static files
    location /static/ {
        alias /var/www/html/tour-booking-ai-agent/backend/app/static/;
    }

    # Proxy to Gunicorn
    location / {
        proxy_pass http://unix:/run/tourbot.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}